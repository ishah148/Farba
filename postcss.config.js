module.exports = {
    plugins: [
        require("postcss-preset-env"),
        // require('cssnano')({   // was replaced for CssMinimizerPlugin
        //     preset: 'default'
        // }),
    ]
}